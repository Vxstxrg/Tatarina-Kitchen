export const layoutConfig = {
	header: {
		height: "60px",
		backgroundColor: "var(--header)",},
	footer: {
		height: "80px",
		backgroundColor: "var(--header)",
		textColor: "var(--text)",
	},
} as const;